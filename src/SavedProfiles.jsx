import { ArrowLeft, ArrowRight, BriefcaseBusiness, CalendarDays, FolderOpen, MapPin, UserRound } from 'lucide-react';

export default function SavedProfiles({records,onOpen,onBack,onRemove}) {
  return <main className="content-page counsel-page"><button className="back-link" onClick={onBack}><ArrowLeft size={18}/>Home</button><div className="page-heading"><div><div className="eyebrow"><FolderOpen size={15}/>RETURNING BENEFICIARIES</div><h1>Saved livelihood profiles</h1><p>Continue reviewing a profile saved on this device.</p></div><span className="prototype-badge">Private to this browser</span></div>{!records.length?<section className="no-match"><h2>No saved profiles yet</h2><p>Complete and confirm an assessment to save it here.</p></section>:<div className="saved-profile-list">{records.map(record=><article key={record.id}><div className="saved-avatar"><UserRound size={22}/></div><div><div className="saved-title"><h2>{record.profile.name||'Unnamed beneficiary'}</h2><span>{record.status==='submitted'?'Roadmap saved':'Profile draft'}</span></div><p><BriefcaseBusiness size={15}/>{record.profile.currentOccupation||'Occupation pending'} · {record.profile.yearsExperience||0} years</p><p><MapPin size={15}/>{record.profile.city||record.profile.district||'Location pending'}</p><small><CalendarDays size={14}/>Updated {new Date(record.updatedAt).toLocaleString()}</small>{record.reference&&<b className="saved-reference">{record.reference}</b>}</div><div className="saved-actions"><button className="primary" onClick={()=>onOpen(record)}>Review again<ArrowRight size={16}/></button><button onClick={()=>onRemove(record.id)}>Remove from this device</button></div></article>)}</div>}<p className="catalog-warning">Profiles are stored only in this browser for the prototype. A production government deployment should use authenticated, consent-based storage and retention rules.</p></main>;
}
